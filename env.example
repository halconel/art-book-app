# Environment Variables Template for Beyond Home Application
# 
# Copy this file to .env and update the values as needed
# cp env.example .env

# Database Configuration
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Rails Environment
RAILS_ENV=development

# Encryption key for attr_encrypted gem (generate a secure 32+ character key)
ENCRYPTION_KEY=your_32_character_encryption_key_here_12345

# Note: For macOS users, add "export DISABLE_SPRING=1" to your ~/.zshrc or ~/.bashrc 
# to globally disable Spring preloader and avoid fork() compatibility issues

# Figma Integration
# Get your personal access token from: https://www.figma.com/developers/api#access-tokens
FIGMA_API_KEY=your_figma_personal_access_token_here

# Optional: Database URL (alternative way to configure database)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/beyond_home_development
